rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Admin SDKからのアクセスのみ許可（クライアントから直接書き込み不可）
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
